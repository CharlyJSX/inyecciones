

<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Aplicación de Inyecciones</title>
    <!-- NAVIGATION -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

   <link rel="stylesheet" href="/src/public/style.css">
    
  </head> 
  <body>

    
 <% if (data) { %> <% for(let i = 0; i < data.length; i++) {
          %>

<div class="card" id="pokemonCard">
  <img src="" alt="Imagen de Pokémon" id="pokeImage">
  <div class="card-content">
    <h3 id="pokeName">Cargando... 2<%= (i + 1) %> <%= data[i].name %></h3>
    <p><%= data[i].time %></p>
  </div>
  <div class="card-buttons">
    <button class="edit-btn" onclick="editarCard()">Editar</button>
    <button class="delete-btn" onclick="eliminarCard()">Eliminar</button>
    <button class="whatsapp-btn" onclick="enviarWhatsApp()">WhatsApp</button>
  </div>
</div>

 <% } %> <% } %>




     <div class="col-md-5">
      <div class="card-body">
        <form action="/inyeccionesSave" method="POST">
          <input
            type="text"
            name="name"
            placeholder="Name"
            class="form-control mb-2"
            autofocus
          />
          <input
            type="number"
            name="mes"
            placeholder="Mes"
            class="form-control mb-2"
          />
          <input
            type="date"
            name="time"
            placeholder="Fecha"
            class="form-control mb-2"
          />
          <input
            type="text"
            name="phone"
            placeholder="Teléfono"
            class="form-control mb-2"
          />
            <input
            type="email"
            name="email"
            placeholder="Correo Eléctronico"
            class="form-control mb-2"
          />

          <button type="submit" class="btn btn-info">Guardar Usuaria</button>
        </form>
      </div>
    </div>
  </div>
</div>

<br>
<br>



 <% if (data) { %> 
  <% for(let i = 0; i < data.length; i++) {%>

         

 <div class="card">
    <img id="profileImage" src="https://via.placeholder.com/120" alt="Foto de perfil" />
    <h2 id="name"><%= (i + 1) %> <%= data[i].name %></h2>
    <p><%= data[i].time %></p>
    <div class="buttons">
      <button class="btn-editar">Editar</button>
      <button class="btn-eliminar">Eliminar</button>
      <button class="btn-whatsapp" onclick="enviarWhatsApp()">WhatsApp</button>

          <a href="/inyeccionesEdit/<%= data[i].id %>" class="btn btn-info">
                Edit
              </a>
              <a href="/inyeccionesDelete/<%= data[i].id %>" class="btn btn-danger">
                Delete
              </a>
    </div>
  </div>



    <% } %> <% } %>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-7">
      <table class="table table-dark table-bordered table-hover">
        <thead>
          <tr>
            <td>N°</td>
            <td>Name</td>
            <td>Mes</td>
            <td>Time</td>
            <td>Phone</td>
            <td>Emal</td>
            <td>Actions</td>
          </tr>
        </thead>
        <tbody>
          <% if (data) { %> <% for(let i = 0; i < data.length; i++) {
          %>
          <tr>
            <td><%= (i + 1) %></td>
            <td><%= data[i].name %></td>
            <td><%= data[i].mes %></td>
            <td><%= data[i].time %></td>
            <td><%= data[i].phone %></td>
            <td><%= data[i].email %></td>
            <td class="d-flex gap-2">
              <a href="/inyeccionesEdit/<%= data[i].id %>" class="btn btn-info">
                Edit
              </a>
              <a href="/inyeccionesDelete/<%= data[i].id %>" class="btn btn-danger">
                Delete
              </a>
            </td>
          </tr>
          <% } %> <% } %>
        </tbody>
      </table>
    </div>
   
 <% if (data) { %> <% for(let i = 0; i < data.length; i++) {
          %>


<div class="card" id="pokemonCard">
  <img src="" alt="Imagen de Pokémon" id="pokeImage">
  <div class="card-content">
    <h3 id="pokeName">Cargando... 2<%= (i + 1) %> <%= data[i].name %></h3>
    <p><%= data[i].time %></p>
  </div>
  <div class="card-buttons">
    <button class="edit-btn" onclick="editarCard()">Editar</button>
    <button class="delete-btn" onclick="eliminarCard()">Eliminar</button>
    <button class="whatsapp-btn" onclick="enviarWhatsApp()">WhatsApp</button>
  </div>
</div>

 <% } %> <% } %>

<script src="./formato.js"></script>

</body>
</html> 
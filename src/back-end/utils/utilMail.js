// enviarCorreo.js

// Función de recordatorio de inyección temporalmente desactivada
export async function sendInjectionReminder({ email, name, nextDate }) {
  // Nodemailer desactivado temporalmente. Aquí iría el envío real.
  console.log(`[Simulación] Recordatorio para ${name} (${email}) el ${nextDate}`);
}
